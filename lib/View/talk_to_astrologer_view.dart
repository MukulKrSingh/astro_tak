import 'package:flutter/material.dart';

class TalkToAStrologerView extends StatelessWidget {
  const TalkToAStrologerView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text('View to show '),
    );
  }
}
